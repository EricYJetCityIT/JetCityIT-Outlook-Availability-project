# Claude Code session files
.claude/

# Environment / secrets
.env
.env.local
.env*.local
*.env

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Node (if added later)
node_modules/
npm-debug.log*

# Azure Functions local secrets (real Cosmos DB connection string)
api/local.settings.json

# Local-only files containing REAL JetCityIT data — never commit to the public repo
jetcity-combined.local.html
dispatch-calendar.html
dispatch-calendar-artifact.html
*.local.html
